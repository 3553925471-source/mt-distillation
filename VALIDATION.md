# v1.0.0 本地验证记录

日期：2026-09-21。环境：Windows、本机 Python 3.14、matplotlib 3.11.2。不是对所有 Python/操作系统版本的验证声明。

- 7 项 unittest 全部通过：7-15 数值回归、总量/轻组分衡算、操作线交点、相平衡、不同进料热状态、进料量缩放、错误工况、Origin 缺失处理、中文空格路径和已有目录保护。
- 将整个 Skill 单独复制到 C 盘含中文和空格的路径，在全新虚拟环境中使用 bootstrap 安装基础依赖。确认没有 originpro，仍完成 7-15 计算和 PNG/SVG 输出。
- 使用全局 Python 调用该独立 Skill，自动切换到 Skill 内的虚拟环境并成功计算。
- Tk 窗口自动检查通过：触发计算按钮、读取逐级数据并加载图片。
- 使用 pythonw.exe 调用 launch.py 的隐藏窗口检查通过，退出码为 0，并保存了 UTF-8 日志。
- 使用本机已安装的 Origin 完成新版 --origin 导出，进程正常结束，OPJU 和 PNG 均生成。人工查看导出图确认：N_trays=13.276、Feed=8、编号 1–14 位于水平阶梯线下方、末个再沸器等效阶梯不编号。
- skill-creator 的 quick_validate.py 检查通过。

7-15 使用精确 xW=(0.35-0.34*0.94)/(1-0.34)，R=2.9122101396307274：塔内理论板数 13.2758，完整板数 14，进料板 8。默认 GUI 示例的完整塔板数为 10，与 7-15 输入不同。

仓库包含 Windows Python 3.10/3.12/3.14 的 GitHub Actions 测试配置；这些云端任务须上传后才能实际运行。本地记录不等同于已完成远程 CI 或其他 AI 客户端的安装验证。未安装/无许可的 Origin 分支以模拟导出失败验证，基础独立安装已在没有 originpro 包的环境中验证。
